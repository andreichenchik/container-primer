# Copy to .env and fill in. The launcher loads .env on startup and forwards
# these into the container for the pi agent. Existing shell env vars win over .env.
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-5.4-mini
